# taken from https://raw.githubusercontent.com/alikins/gitconfig/master/gitconfig
# TODO!!

# TODO: should go in gitconfig-local?
[user]
	email = olalonde@gmail.com
	name = Oli Lalonde

[github]
  user = olalonde

[core]
	excludesfile = /Users/olalonde/.gitignore_global
	pager = less -F -X

[push]
  default = simple

# local user config, usernames, etc
[include]
  path = ~/.gitconfig-local

[color]
  diff = auto
  status = auto
  branch = auto
  interactive = auto
  ui = auto

[alias]
  st = status
  co = checkout
  br = branch -v
  lg = log --graph --color --abbrev-commit --decorate --date=relative --oneline --all
  d = difftool

[merge]
	tool = opendiff
  conflictstyle = diff3

[mergetool]
  prompt = false
[diff]
	tool = vimdiff
[difftool]
	prompt = false
[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
[difftool "sourcetree"]
	cmd = opendiff \"$LOCAL\" \"$REMOTE\"
	path =
[mergetool "sourcetree"]
	cmd = /Applications/Sourcetree.app/Contents/Resources/opendiff-w.sh \"$LOCAL\" \"$REMOTE\" -ancestor \"$BASE\" -merge \"$MERGED\"
	trustExitCode = true
